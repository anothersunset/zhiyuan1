# Project Log

## 2026-08-07T08:42:06+08:00

- Initialized project memory files.
- Awaiting the planning prompt from the user.

## 2026-08-07T08:52:00+08:00

- 修复专业自动联想的数据来源：改用实际有数据的专业分数线表，并保留关键词、地区和科类过滤。
- 登录页顶部栏与业务页统一桌面和移动端尺寸规范。
- 影响文件：`MajorMapper.java`、`frontend/src/styles.css`、静态构建产物及项目状态记录。
- 验证：真实接口返回专业候选，前端构建通过，全量 67 项测试无失败。
- 下一步：用户刷新页面验收联想下拉与顶部栏一致性。

## 2026-08-18 offline recovery checkpoint
- Copied audited backend public API controllers/services/DTOs/mappers into current worktree.
- Repaired current frontend null-safe probability UI and cloud-draft replacement sync.
- Removed misleading hash-derived major cutoff presentation from `MajorDetailView.vue`.
- Added focused offline tests: `ScoreRankControllerTest`, `ProbabilityServiceTest`.
- Added test shims/resources for offline sandbox verification:
  - `src/test/java/org/springframework/data/redis/core/StringRedisTemplate.java`
  - `src/test/resources/mockito-extensions/org.mockito.plugins.MockMaker`
- Verified offline focused tests passed via manual JUnit launcher in sandbox.
- Created checkpoint zips under `/data/zhiyuan-fixed-checkpoint-*.zip`.
- Remaining blockers are sandbox dependency/network limits for full npm/Maven restore.

## 2026-08-20 student-first UX pass
- Explored the reference product from a student task perspective: profile first, then school/major discovery, then one working volunteer sheet, with AI as optional assistance.
- Removed the full volunteer big-data block (statistics, school/major TOP lists, and destination-city ranking) from `VolunteerView.vue`.
- Moved the active top-navigation underline to the bottom edge and vertically centered labels so the line no longer crosses text.
- Unblocked ordinary AI conversation when no plan is selected; nullable plan context is now sent as `null`, not `0`.
- Added automatic local-sheet to cloud-current-draft bridging when a browser has a 45-slot sheet but the user has no current cloud draft.
- Added an explicit, non-blocking plan-context hint in the AI welcome screen.
- Added `UX_REVIEW_20260820.md` with reference-product findings and competition-focused design decisions.
- Validation: Vue/CSS parsed through Prettier, extracted script blocks passed `node --check`, targeted source assertions passed, and local Chromium geometry confirmed the active underline does not overlap navigation text.
- Full Vite build is still blocked in this sandbox because the npm dependency tarballs are unavailable; `npm ci` ends with npm's `Exit handler never called!` error.

## 2026-08-20 functional-design correction
- Restored the original color theme, brand, navigation names, page descriptions, home layout, side panel, login copy, and consultation copy.
- Limited the new pass to functional behavior and data correctness.
- Kept query reactivity and null-safe sorting in `SchoolsView.vue`.
- Removed generated professional-group IDs and fixed missing-value/validation behavior in `ChooseView.vue`.
- Replaced page-local fabricated yearly admission figures with the shared cutoff history source in `VolunteerSheet.vue` and removed fabricated plan/ranking metrics.
- Added explicit invalid-score feedback in the score-segment query.
- Added `FUNCTION_DESIGN_20260820.md` to document the preserved UI boundary and functional changes.

## 2026-09-19 native tool-calling upgrade for agent LLM fallback
- Upgraded the agent LLM fuzzy-intent fallback from "prompt-constrained JSON output" to native function calling (OpenAI-compatible `tools`/`tool_choice`, DeepSeek same shape), closing the long-term fuzzy-semantic-fallback roadmap item.
- `AgentToolRegistry.getOpenAiToolDefinitions()` now derives OpenAI function-calling declarations from `AgentToolSpec` (ranges encoded as JSON Schema `minimum`/`maximum`/`minLength`/`maxLength`), plus a synthetic `reply` tool for direct/clarify responses; new tools need zero changes here.
- `AiChatClient.chatWithTools()` posts `tools` + `tool_choice=auto` (no `response_format`, which is mutually exclusive with function calling on DeepSeek) and parses `choices[0].message.tool_calls[0]`; plain content maps to a reply decision.
- `AgentDecisionService` prefers the native path (`ai.agent.tool-calling.enabled`, env `AGENT_TOOL_CALLING_ENABLED`, default on) with the real-time state snapshot and history compaction unchanged; on native-channel failure it degrades to the legacy JSON mode. A code-level gate now blocks `removePlanItem` without a pending user-request -> assistant-confirmation context in both LLM modes.
- Tests: new `AgentNativeToolCallingTest` (15 cases: schema derivation, tool_calls/reply/content parsing, unknown-tool and malformed-args fallback, JSON-mode degradation, deletion gate, flag off); full suite 187 tests green.
- Deployed to the zhiyuan6 stack (image rebuild + `zv6-backend` recreate). Live verification on port 18080: fuzzy "文科生" phrase produced a state-aware native `reply` clarification (profile says PHYSICS/588); "偏硬核的工科专业" routed natively to `recommendMajors(majorKeyword=工科)` with a profile-aware explanation; strong-intent local planner short-circuit intact.

## 2026-09-19 intent roadmap phase 1-3 (lexicon / dialog state / major catalog)
- **Phase 1 - intent lexicon**: externalized all local-planner synonym lists to `src/main/resources/agent/intent-keywords.json` (27 contains-groups + major dictionary/stopwords/interrogatives/prefix chains), loaded at startup by new `AgentIntentLexicon` (fail-fast on corrupt resource). Code keeps only the combine/exclude boolean structure; three ordered groups (schoolStripVerbs / schoolNamePrefixes / majorKeywordPrefixes) preserve replace-chain order. Contract test `AgentIntentLexiconTest` pins every group name + minimum size. The corpus suite caught a real equivalence bug during migration (prefix-strip group missing from groups map -> "他的热门" leaked as major keyword) before it could ship.
- **Phase 3 - major catalog**: new `AgentMajorCatalogService` caches the 100-row curated `major` table in memory (5-min TTL refresh, failure retry backoff, degrade-to-passthrough on DB error). Normalization wired at the convergence layer (`AgentToolFacade.recommendMajors`/`getMajorOverview`) so both local extraction and native tool-calling keywords benefit: exact hit -> unique containment -> dirty-keyword-wraps-catalog-name (longest, tie = ambiguous = passthrough). Never hard-rejects, preserving the recommendation engine's graceful zero-result fallback. Decision-layer fallback added in `AgentDecisionService`: when regex/dictionary extraction fails, unique longest catalog name found in text fills the keyword ("考古学就业前景怎么样" now routes locally in 0.3s to a real catalog hit, previously fell to the LLM).
- **Phase 2 - DialogState**: the 5 separate reverse scans over `recentMessages` (school slot, delete confirmation, major-direction slot, recommendation snapshot, plan snapshot) merged into one pass in new `AgentDialogState` (plus one-time message-level regex extraction). Each scan's trust boundary and first-hit-stops rule preserved verbatim: school slot checks byName payload before USER text per message; recommendation snapshot stops at the first rec TOOL_RESULT even when its payload is malformed; plan snapshot accepts payload-less TOOL_RESULTs; delete confirmation only trusts the LAST assistant text + its immediate predecessor; direction slot only trusts the last assistant text. The state is consumed by the local planner, the destructive-call gate, and the LLM prompt builder (profile detail map kept verbatim for incomplete profiles). 15 dedicated equivalence tests in `AgentDialogStateTest`.
- Validation: full suite 220 tests green (was 187 before this roadmap). Deployed to zhiyuan6; startup log confirms "lexicon loaded: 27 groups, 40 major keywords".
- **Phase 4 (school aliases) measured, deferred as reviewed**: live LLM tests show unambiguous abbreviations already resolve natively ("湘大" -> 湘潭大学 33 majors, "华科" -> 华中科技大学 27 majors, both via getSchoolDetailByName with real DB hits). The failure case "湖大的专业怎么样" is misrouted LOCALLY to getMajorOverview("湖大的") before the LLM sees it - and "湖大" is ambiguous (湖南/湖北大学), so an alias table would not fix it safely. Building the table stays deferred until after the competition, per the phase-1 review verdict.

## 2026-09-19 school-detail reply card (user-reported reply gap)
- User report: "帮我推荐湘潭大学热门专业" called `getSchoolDetailByName` correctly (that routing is by design from the 2026-09 audit: explicit school name + 推荐/热门 -> the school's major list), but the final reply was just the one-line tool summary "已按学校名查询...可参考 33 个专业，例如：..." instead of the expected major list.
- Root cause: `AgentReplyFormatter.format()` only had branches for overviewMarkdown (getMajorOverview), fallback advice, and topItems (recommendations); school-detail payloads (universityName + majors array) fell through to the raw summary echo.
- Fix: new school-detail branch renders a card - `## 校名`, tier line (985/211/双一流 flags + schoolTags deduped, falling back to universityTier), province, "可参考专业（共 N 个，按参考录取分从高到低展示）" table from the payload's top-8 rows (majorName/cutoffScore/minRank, nulls as "—"), and next-step guidance (ask major overview / add to plan after recommendation). Major list ordering comes from the DB query (latest year, cutoff DESC), so "录取分较高≈报考热度更高" is data-grounded, not fabricated.
- Also: native tool-calling calls whose content is empty (DeepSeek often sends no content with tool_calls) now get a deterministic per-tool preamble (same wording as the local planner, parameterized by args) instead of the bare "正在调用工具。" chip.
- Tests: new `AgentReplyFormatterTest` (6 cases: card rendering, tier fallback, empty-majors summary fallback, ordinal payload, recommendation/overview branches unaffected) + preamble case in `AgentNativeToolCallingTest`. Full suite 227 green.
- Deployed and verified live with the exact reported phrase: 0.24s local-route -> tool_call preamble -> result -> 415-char markdown card with the 8-major table.

## 2026-09-20 national corpus expansion wave 1+2 (dual-agent: import + audit)
- Executed the corpus-expansion plan with a dual-agent setup: main agent ran the import pipeline; an independent read-only auditor subagent reviewed each wave (baseline integrity / data quality / API application effect) before work continued.
- Source: Gaokao-Compass-11M (MIT, HuggingFace choucsan/Gaokao-Compass-11M), 2025 edition, 6 provinces (guangdong/jiangsu/henan/hubei/hebei/sichuan), 4 tables (score-range / school-admission / major-admission / enrollment-plan), ~56MB CSVs under tmp/gaokao-compass/.
- Safety: mysqldump baseline backup at tmp/db-backup-20260920/ before any write; imports are idempotent (NOT EXISTS for university, ON DUPLICATE KEY UPDATE elsewhere); Hunan SIMULATED data (2613 rows) untouched as the competition-verification showcase; new rows data_kind='REAL' (frontend labels them 录取数据 vs 比赛验证数据).
- Subject mapping: 物理类→物理 / 历史类→历史 (all 6 provinces are new-gaokao in 2025; art/mixed batches excluded; school-level cutoff deduped by batch priority 本科批>提前批>特殊类型>专科, using MIN(score) semantics per (university, subject, province)).
- Imported (baseline → now): university 80→1445 (name-deduped, full/half-width parens normalized; tier 本科批1181/专科批204 from batch); admission_cutoff 405→7702 (school line per province×subject, min over groups); score_rank_mapping 3405→9686 (2025 curves, segment_count included); major_admission_cutoff 2613→79183 (76,570 REAL major lines; plan_count/duration_years/tuition_per_year 93% coverage via enrollment-plan join on (校代码,归一组号,归一专业名) with (校代码,专业名) fallback; 34,619 rows linked to the curated major table via name).
- Pipeline scripts: tmp/gaokao-compass/w1_import.py, w2_import.py (CSV→SQL, stdout-only, re-runnable).
- Verification (auditor rounds): R1 PASS 12/13 (the "miss" was a wrong expectation in my own checklist: /api/plans/current 404 for an account without a draft is correct business behavior); R2 PASS 13/13, zero issues. Evidence highlights: Hunan baselines intact (405/3405/2613 SIMULATED); zero duplicate identity keys; zero orphan REAL rows; no out-of-range scores. Live API: 广东/物理 withDataOnly total=475; 深圳大学 detail cutoffHistory 2025=572/16789 + majors 52 items all with plan/duration/tuition (计算机 634/8073/24人/4年/6853元); agent chat "查看深圳大学详情" renders the card from REAL data in 0.16s; recommendation engine 广东580物理 → 冲16/稳19/保409.
- Known notes: (1) 河北/湖北/四川 2024 score-range files lack category, so 2024 was skipped this round - add later for cutoff-history depth; (2) comprehensive-reform provinces (浙江/上海 etc., category=综合) cannot map to subject_type 物理/历史 and were excluded; (3) same-name major lines from different 专业组 appear twice in school detail (upstream query behavior, pre-existing); (4) static university attributes (双一流/软科排名/保研率/研究生院) not in this dataset - separate public-list supplement planned; (5) test accounts added: gdreviewer01 (广东/物理/580), tcsmoke0919 province corrected Hunan->湖南 earlier.

## 2026-09-20 Hunan competition data replaced with real dataset (wave 3)
- Per user decision, all Hunan SIMULATED competition-verification data was replaced with real Gaokao-Compass-11M data (2024+2025, both years clean 物理类/历史类):
  - Deleted: score_rank_mapping 湖南 3405 rows (incl. the fabricated 2026 year), admission_cutoff 湖南 405, major_admission_cutoff SIMULATED 2613, admission_group_cutoff 湖南 804 (table has no backend consumers; left empty rather than importing fabricated group rows).
  - Imported real Hunan: major_admission_cutoff +40,273 REAL rows over 2024/2025; admission_cutoff 湖南 2024=2941/2025=1440; score_rank_mapping 湖南 2024/2025 各 1152; university 1445→1902 (Hunan admits include many out-of-province schools).
- Whole-database state: major_admission_cutoff 116,843 rows ALL data_kind='REAL' (SIMULATED count = 0) — the 比赛验证数据 label no longer appears anywhere; everything is labeled 录取数据 with calibration_source='Gaokao-Compass-11M (MIT)'.
- Pipeline: tmp/gaokao-compass/w3_replace.py (scoped DELETEs + idempotent re-import, stdout SQL).
- Audit (independent read-only subagent, round 3): PASS 13/13 zero issues — baselines for the other 6 provinces untouched (admission_cutoff 7297 / srm 6281 / 广东 total=475), no duplicate identity keys, no orphan rows, cutoff range 184-692, fabricated 2026 fully gone (score-rank API now reports mappingYear=2025).
- Live smoke with Hunan profile (588/物理): rank lookup 21502 EXACT; recommendation 冲27/稳39/保692 all REAL; Xiangtan University detail card shows real 2024+2025 cutoff history and 71 majors all with planCount/tuitionPerYear (数学类 97人/5300元).
- Fallback backups: tmp/db-backup-20260920/ (baseline + pre-replace dumps). Simulated Hunan data remains recoverable from pre-replace-hunan.sql if the competition showcase needs it back.

## 2026-09-20 frontend display completion wave (W4/W5, dual-agent audited)
- Goal: fill the remaining frontend display gaps identified in the phase-1 inventory.
- **W4a university attributes** (source: ShanghaiRanking public API bcur main list, 589 schools): soft_ranking now 504 rows (Tsinghua=1, Xiangtan=107), school_type 610 (综合/理工/师范/农林…), is_double_first_class=117, tags backfilled as "985|211|双一流|综合类" style (COALESCE — existing hand-curated tags preserved; flags GREATEST-merged).
- **W4b major catalog** (source: MOE 普通高等学校本科专业目录（2024年）official PDF, parsed with pypdf): major table 100 → 657 (+557 rows across all 13 学科门类, each with category/degree_type and a factual template description; subject_requirement NULL by design — course selection is per-school). MajorsView tree now covers all 13 categories; MajorDetailView works for the full catalog.
- **W4c + W5 history depth & orphan fix**: imported 2024 school lines for 广东/江苏/湖北/四川 (admission_cutoff 2024: 384/2338/1803/1123) so cutoffHistory shows two years. Round-4 audit FAILed one item: 106 广东 schools with only-2024 lines entered the list (total 475→581) but had EMPTY majors tables (no 2024 major lines had been imported). Root cause verified in code: UniversityQueryService.detail majors query (findAllByUniversityAndProvinceSubject) has NO year filter — so the fix was to import, not revert: **W5** imported 4-province 2024 major lines (+47,185 REAL rows; major_admission_cutoff now 163,928 all REAL: 2024=73,715 / 2025=90,213). Same-major dedup keeps latest year, so 2025 school cards are unchanged and orphan schools (e.g. 三峡大学科技学院) render complete cards (5 majors, 2024).
- Audit trail: R4 11/12 (C11 FAIL) → fix W5 → R4-re PASS 4/4 (independent re-check: lists, orphan detail, no duplicate identity keys, no out-of-range scores, untouched tables intact).
- Remaining known gaps (documented, not fabricated): (1) postgraduateRate / hasGraduateSchool / hasDoctorProgram have no clean structured source (MOE degree-authorization lists are dynamic approval documents); they render as "—" in school detail; (2) ~35 双一流 art/military/sport academies are not in the ShanghaiRanking main list and mostly not in普通批 admission data, so is_double_first_class covers 117 of 147; (3) MOE catalog parse captured 615 of ~792 majors (cross-page line breaks) — the majors users actually encounter in admission data are covered; (4) 医药/财经类单列榜 (ShanghaiRanking sub-lists) not fetched — their schools lack school_type.
- Pipelines: tmp/w4_frontfill.py, tmp/gaokao-compass/w5_2024majors.py (re-runnable, stdout SQL). Pre-change backup: tmp/db-backup-20260920/pre-replace-hunan.sql still valid as the day's rollback point.

## 2026-09-20 W6 university attributes completion + schoolTags serialization fix
- **W6a school types & flags** (source: 掌上高考 public school-list API api.zjzw.cn, 2,991 schools paginated): university.school_type standardized across all 12 categories (综合614/理工554/医药174/师范169/财经144/农林60/艺术65/政法33/语言27/民族12/体育17/军事7, coverage 1876/1902, suffix-normalized to match W4a style), nature backfilled, 985/211/双一流 flags GREATEST-merged (双一流 117→158, incl. art academies like 中央美术学院 and branch campuses), tags recalculated to standard "985|211|双一流|综合类" format.
- **W6b ranking refresh**: soft_ranking re-based to the ShanghaiRanking full main list (bcur_type=10, 812 schools; 590 matched; Tsinghua=1). Note: bcur_type=11 used in W4a was a sub-list; type=10 is the authoritative full ranking.
- **Backend fix (audited FAIL → fixed → re-audit PASS)**: R5 audit found ordinary schools (non-985/211/双一流) rendered empty schoolTags arrays because buildSchoolTags only emitted hierarchy tags. Added a tagsColumn-aware overload (hierarchy tags per the 20260820 "211≡双一流" design + type segments parsed from the standardized tags column, filtering "211"); all 5 call sites (UniversityQueryService×2, RecommendationService×2, SchoolDetailService) now pass the tags column. A buildSchoolTags mutual-recursion bug introduced mid-fix was caught by the test suite (StackOverflowError in RecommendationServiceTest) before deploy. One legacy test assertion updated to the new contract (ordinary schools: no hierarchy tags, type segment allowed). Full suite 227 green; rebuilt and deployed.
- Live verification: 广东/物理 withDataOnly total=581 with ZERO empty schoolTags (was 50/100); 北京大学医学部=["985","双一流","医药类"]; 中国刑事警察学院=["政法类"]; 湘潭大学=["双一流","综合类"]; 中央美术学院=["双一流","艺术类"].
- **Out-of-scope data drifts detected by the auditor and preserved**: major_admission_cutoff now carries 2,248 additional SIMULATED rows labeled 湖南比赛验证/演示值 (admin-added showcase data — not touched); score_rank_mapping grew +1,141 rows of Hunan official 一分一段 incl. 2026 (admin-imported). Stale Redis score-rank caches from before the 2026 import were cleared; /api/meta/rank 湖南/物理/588 now returns mappingYear=2026 rank=25204 EXACT consistently.
- **Still open**: postgraduateRate / hasGraduateSchool / hasDoctorProgram remain unpopulated — the 掌上高考 detail endpoint is deprecated ("接口请求版本不存在") and no clean structured source exists; fields render as "—". Pipelines: tmp/w6_attrs.py (re-runnable), raw pages under tmp/univ-attrs/eol/.

## 2026-09-20 frontend UX fixes (employment map / related majors / page size / SPA fallback)
- **Employment directions per major**: MajorDetailView previously used a 9-entry category-level EMPLOY_MAP (土木工程 showed 软件开发/算法工程师 — wrong category default). Replaced with a keyword-driven mapping (~30 keyword groups covering all 13 categories + 专科, e.g. 土木/建筑 → 结构设计/施工管理/造价咨询/工程监理/BIM), falling back to category defaults. Verified live: 土木工程 now renders 结构设计/施工管理/造价咨询/工程监理/BIM 工程师.
- **Related major recommendations**: same-category candidates are now sorted by openSchoolCount desc (hot-first) instead of insertion order. 土木工程 now recommends 计算机科学与技术/数据科学与大数据技术/电气工程及其自动化 etc. instead of 丝绸设计与工程/乳品工程.
- **Major catalog pruning for selectors**: GET /api/majors gained `minOpen` (default 20) and `all` params — default list now returns only majors with ≥20 opening universities (657→219; top: 计算机科学与技术 748 所). MajorsView/SchoolsView dropdowns shrink accordingly; MajorDetailView falls back to /api/majors?all=true when a deep link targets a pruned major.
- **List loading speed**: root cause was the frontend looping up to 15 sequential size=100 requests while the backend recomputed the whole list per request. Raised MAX_PAGE_SIZE and @Max validation to 500; HomeView/SchoolsView/EnrollPlanView now use size=500 with fewer loops (7 provinces ≤2 pages). Measured: one 500-item page = 0.79s; a full province list now ~2 requests instead of 15.
- **SPA history-route fallback**: direct visits/refreshes of sub-routes (/rank, /schools/41, /majors/土木工程…) returned RESOURCE_NOT_FOUND JSON (blank page / bare error box) because Spring Boot had no index.html fallback. Added SpaForwardController forwarding the whitelisted frontend routes to /index.html; /api and dotted static paths unaffected. Verified /rank now renders the app (hash-router lands on home; /#/rank renders the rank empty-state page).
- Backend tests 227 green. Deployed to zv6-backend. Frontend builds inside the image.

## 2026-09-20 rankings page implemented + browser-verified UX fixes
- **/#/rank now displays a real leaderboard**: previously the page was a deliberate empty state ("本页不展示本地生成榜单") because no traceable ranking data existed. After W6 imported the ShanghaiRanking main list (soft_ranking, 590 schools), added GET /api/universities/ranking?schoolType= (UniversityMapper.findRanking, tinyint→Boolean safe mapping) and rebuilt RankView.vue: source-provenance note, 13 school-type filter tabs, table (排名/院校/省份/类型/层次标签), pagination, links to school detail. Fixed a class-name collision with legacy `.gk-rank` flex-row styles in styles.css (renamed container to `gk-rankboard`), and a ClassCastException (tinyint(1) mapped to Boolean) caught live.
- Live: /#/rank renders 590-school board (1 清华 / 2 北大 / 3 浙大…), type filter works (医药类 2 所 — medical schools largely absent from the 7-province admission corpus), each row links to school detail.
- Closed the previous "未找到该专业" mystery: the deep-link test URL had a typo (%E5%9C%9C=圜 instead of %E5%9C%9F=土, i.e. 圜木工程), API and matching were correct. Correct-encoded /#/majors/土木工程 verified: intro renders, employment = 结构设计/施工管理/造价咨询/工程监理/BIM, related = hot majors by openSchoolCount.
- Full suite 227 green; deployed.

## 2026-09-20 volunteer sheet fake-data purge (user-reported)
- User report: 推荐卡片 "可填专业(0)" yet the card showed 14 majors; volunteer-sheet major dropdown for 西北工业大学 showed unrelated majors (中医学/临床医学/互联网金融…); "[1164组]" label looked like a real group code.
- Root causes — all legacy decorative fake data in the volunteer-sheet era components:
  1. `RecommendSchoolRow.vue`: `fillableMajors = min(school.majorCount, seed % 15 + 5)` — a seed RANDOM number; plus metaLine fabricated 硕士点/保研率/软科排名, groupNo/schoolCode/subjectReq fabricated, and years fabricated 2025/2024 scores-plans.
  2. `VolunteerSheet.vue`: major dropdown `majorOptions` = whole /api/majors catalog filtered by batch only (NOT per school) — hence unrelated majors; "可填专业(N)" used `f.majorCount` (recommendation item field = 0/null) while the expanded list below used the real per-school API; "[XX组]" label = String(school.id).padStart + "组" (university id disguised as a group code).
- Fixes (frontend only):
  - VolunteerSheet: dropdown now per-school via `majorsForRow(slot)` reading majorCache (real /api/universities/{id} data, batch-filtered, lazy-loaded); removed fake [组] span; count = real majorList.length.
  - RecommendSchoolRow: removed seed-fabricated computeds (groupNo/schoolCode/subjectReq/fillableMajors/ fabricated metaLine segments); metaLine now uses real item fields (province/tier/schoolTags); years table shows ONLY the item's real admission year row (plan from planCount, "—" when absent) — no fabricated 25/24 numbers; rankCompare null-safe.
- Data-source verification: /api/universities/1164 (湖南/物理) returns 16 real NWPU majors (计算机/航空航天类/工程力学/柔性电子学/海洋工程类…), zero unrelated. el-select synthetic-keyboard automation limitations prevented full E2E dropdown interaction in the harness (real mouse unaffected); data layer verified by API.
- Frontend rebuilt + deployed; backend untouched this round.

## 2026-09-20 fillable-major count truly fixed (aggregation year window)
- User re-reported 可填专业(0) still visible. Verified NOT a cache issue: fresh bundle reproduced it — real remaining bug.
- Root cause: aggregatePlanByUniversity (list API majorCount/planCount source) filtered by province-wide MAX(admission_year); the admin-imported 2026 Hunan demo rows (2,248 SIMULATED) pushed the window to 2026, zeroing counts for every real 2024/2025 school. Frontend card count bound to the lazily-loaded per-school list (empty until clicked), so (0) everywhere.
- Fix: per-university latest-year correlated subquery in the aggregation SQL; frontend count = loaded list || backend majorCount || 0. Verified: list API returns real counts (北大 10 majors/319 plan); volunteer sheet cards show 5/11/17/30/37, zero (0). Also logged as DEV_ISSUES L-06 (the L-03 year-window hazard materialized).
- 227 tests green; rebuilt & deployed. NOTE for user browsers: previously-opened tabs hold the old SPA in memory — one F5 refresh is needed to pick up the fix.

## 2026-09-20 schools list performance + visible failures
- User reported 查大学 slow and showing 院校 0 所. Measured: API ~1.9s/page (Hunan/physics/690, total=1630) — not a server timeout; the "0 所" was the OLD frontend JS (pre-refresh, 15×100 sequential requests) blowing through the 15s apiFetch timeout, with the error swallowed into the "no results" empty state (loadError was assigned but never rendered).
- Fixes: (1) UniversityQueryService.list() now caches the sorted full item list (keyed by all query params except page/size, TTL 3min, ≤64 entries) and the heavy SQL context (latest-cutoff map + plan aggregation, keyed by province+subject, TTL 3min) — page 2+ and repeat visits drop from ~1.9s to 0.04-0.06s. (2) size cap raised to 1000 (@Max + MAX_PAGE_SIZE); HomeView/SchoolsView/EnrollPlanView use size=1000 with ≤3-page loops (1,630 schools = 2 requests). (3) SchoolsView now renders loadError ("院校列表加载失败：…") and a loading state, so failures are no longer disguised as empty results.
- Verified live: cold 1.85s, warm 0.06s, total=1630, page size 1000. 227 tests green; deployed. Logged as DEV_ISSUES L-07.
- Reminder to user: browsers holding the pre-deploy SPA in memory need one F5 refresh to load the new bundle (index.html is no-cache, assets are hashed).

## 2026-09-21 calibrated routing layer (Jev "System One" principles, default OFF)
- Goal: harden the agent decision layer with Jev's typed-decision/calibrated-probability principles (fuzzy intent routing + paraphrased delete confirmation) WITHOUT changing default behavior; every path reversible.
- New `service/agent` classes: `CalibratedRouteProvider` (abstraction), `DeepSeekChoiceProvider` (Choice/Noul semantics with calibrated confidence over the existing DeepSeek channel; strict JSON; unknown-tool/blank/invalid → "no opinion"), `JevSystemOneProvider` (TypeSafe AI hosted endpoint, inert without jev.api-key — wire format per 2026-09 docs, MUST be shadow-verified before any active use), `SemanticRouterService` (orchestrator), `AgentRoutingPreambles` (extracted from AgentDecisionService to avoid a constructor cycle).
- SemanticRouterService safety design: shadow mode (side-by-side comparison log only, never decides) behind `ai.agent.calibrated-routing.enabled` (default false); active mode short-circuits ONLY on confidence ≥ act-threshold (clamped [0.5,0.99], NaN→0.9) AND whitelisted business tool (reply never short-circuits); dedicated bounded executor (core2/queue32/AbortPolicy, daemon, @PreDestroy) so hung upstream calls can never occupy ForkJoinPool.commonPool; 4s route budget; breaker opens on 3 consecutive failures OR 3 consecutive no-opinions (garbage-JSON blind spot closed), 60s cooldown, single half-open probe that re-opens immediately on failure (incl. no-opinion probes); provider exceptions/timeouts/low confidence always fall back to the legacy lexicon→DeepSeek path (worst case = the layer absent).
- Delete semantic confirm: `ai.agent.delete-semantic-confirm.enabled` (default false, independent switch) — Noul gate inside guardDestructiveCalls, TRUE only at ≥0.9 calibrated confidence, fail-closed everywhere else; legacy two-pass explicit confirmation untouched.
- Tests 227→272 green (router gating/threshold clamps/breaker/half-open/shadow/provider parsing/Jev delete-gate pure function; flaky timeout test made deterministic with settle-polling).
- Audit: TWO independent subagent review rounds. Round 1: P1-1 (route calls on ForkJoinPool.commonPool + timeout not cancelling hung upstream → pool starvation risk) and P1-2 (Jev deletion gate missing confidence check, violated fail-closed) — both fixed; round 2 verified fixes and found 4 new P2s, of which rejection double-counting, no-opinion half-open, flaky test, and missing Jev-gate tests were fixed immediately. Accepted P2s: one shared breaker across route/shadow/delete (fail-safe direction), synchronous shadow cost (documented in class javadoc), steady-state 2 route threads (queue-before-max semantics), double warn logs on Jev call failure.
- Deployed to zv6-backend with ALL new flags OFF → behavior identical to previous release. Startup log confirms "Jev System One provider disabled (no jev.api-key configured)". Live smoke: local-intent "帮我推荐学校" → recommendSchools tool → real Hunan/physics/588 recommendation (rank 25204, 38 schools, 冲/稳/保 groups); fuzzy chat → DeepSeek native tool-calling reply in 1.36s; zero errors in logs.
- Enable path (when ready): ① shadow: set AGENT_CALIBRATED_ROUTING_ENABLED=true (mode defaults to shadow) and watch "Agent calibrated route shadow: semantic=… actual=… agree=…" logs for agreement rate; ② active: AGENT_CALIBRATED_ROUTING_MODE=active (start at threshold 0.9); ③ delete confirm: AGENT_DELETE_SEMANTIC_CONFIRM_ENABLED=true; ④ real Jev: set JEV_API_KEY + AGENT_CALIBRATED_ROUTING_PROVIDER=jev, shadow first (wire format unverified). Runtime rollback = flip env flag off, no redeploy needed. Code rollback = git checkout the touched files (baseline had no uncommitted agent changes).
- Smoke account smoketest_jev (user id 36) left in DB for shadow-mode observation.
- Addendum (same day, user decision): removed the TypeSafe AI hosted-endpoint adapter entirely — deleted `JevSystemOneProvider` + its test, dropped the `jev:` config block and `provider=jev` option. Rationale: borrow Jev's PRINCIPLES only (typed Choice/Noul + calibrated confidence + confidence gating), no external model service, no adapter-compatibility risk. The calibrated layer now runs solely on the project's own DeepSeek channel (`DeepSeekChoiceProvider`); `CalibratedRouteProvider` remains as the extension seam if a second backend is ever needed. Tests 272→266 green; rebuilt and redeployed to zv6-backend; startup log has zero Jev-endpoint references; live smoke (local-intent recommendSchools + LLM-formatted reply) normal. Flags remain default-off; enable/rollback path unchanged.

## 2026-09-21 evening: proactive bug-hunt wave (4 pattern-scan subagents + live UI testing) + failure-rewrite design
- User mandate: stop waiting for user-reported bugs; subagents scan for L-01~L-08 family patterns per module while the main agent does real browser click-through testing; mismatches go to root-cause subagents.
- Scans (4 subagents, evidence-backed): 32 findings (3 P0 / 8 P1 / 21 P2). UI live testing: 8 scenarios (home/schools list/school detail/major detail/rank+refresh/recommendation flow/plan add/agent chat) — plan-add flow verified end-to-end (was suspected broken, dialog confirmed working).
- FIXED this batch (backend): ① findSchoolsByMajorName global MAX(year) poisoned by 13 Hunan-2026 demo rows → per-(university,province,subject) correlated window (临床医学 schools list: was demo-only, now 100 real schools each at own latest year); ② UniversityQueryService majorId filter resolving empty now returns empty result instead of silently unfiltered (L-08 family); ③ SchoolDetailService.getSchoolDetailByName fuzzy resolution (exact→contains: unique adopt / multi→candidates 400 / none→not-found) — fixes "查中南大学" verb-glued name failing as 暂未收录; ④ decision-layer: delete-confirm negation guard (先不/不用/不确认…), removePlanItem without/unparseable selectionIndex asks user instead of fabricating "第1个", JSON-mode salvages reply when action missing + strips ``` fences; ⑤ AgentFailureRewriteService (user-specified design, default ON via ai.agent.failure-rewrite.enabled): read-only tool hard failures are rewritten by the LLM with general knowledge (labeled 非本系统数据, forbidden to fabricate scores/ranks), replacing red errors; plan-mutation tools keep deterministic guidance; fail-open to legacy text.
- FIXED this batch (frontend): ChooseView size=100→full pagination + loadError render (P0: 93% of candidates silently dropped); VolunteerSheet pagination + visible error toast; EnrollPlanView/SchoolsView/HomeView removed hard 3-page caps + resp.ok checks + error states; AgentWorkspace failed-tool chip softened to neutral "系统数据未命中，已由 AI 补充解答" when an AI answer follows (no more 爆红); deleted dead mock exploreData.js (L-05).
- Tests 271→278 green; independent audit round: deployable, no P0; its P1 (SchoolsView missing resp.ok) fixed in-batch. Live verified: user's original failing message now resolves 中南大学 (52 majors deduped); 哈佛大学 (not in dataset) gets graceful LLM answer with 通用信息 label instead of red error; 临床医学 opening-schools list restored.
- Deferred pending user decision / next batches: (a) admission_cutoff recommendation window is province-global MAX(year) and currently reads the 2026 demo rows — changing to per-university window would alter demo behavior (needs user call); (b) 2026 SIMULATED demo rows equal to the demo user's score (588/25204 across multiple schools) surface on recommendation cards as 最低位次 — data governance call; (c) addPlanItem ignores named school (把中南大学加入志愿单 adds item 1) — needs by-name matching in executor; (d) RocketMQ async recommendation pipeline is dead code from the product flow (failure invisible); (e) prod profile DB_SCHEMA_INIT_MODE defaults to always with 13 DROP TABLEs in schema.sql — flip default to never; (f) P2 backlog: LIKE escaping in fuzzy resolver, SchoolsView major-filter silent-null branch, MajorDetailView hardcoded 2025 title, display-layer SubjectType.name() remnants, MajorPickDialog redundant dedup.

## 2026-09-21 night: bug-hunt workflow persisted + full backlog cleared
- Fixed workflow: `zhiyuan-bug-hunt` (.zcode/workflows/zhiyuan-bug-hunt.dwf.ts) — 4 parallel pattern-scanners (data/frontend/agent-decision/assembly per DEV_ISSUES L-01~L-08) → shared triage → independent per-finding confirmation → bounded fix rounds gated by `mvnw.cmd -B test` → independent diff audit → docker build/up → live smoke → DEV_ISSUES/PROJECT_LOG write-back. Browser click-testing stays with the main agent outside the workflow. Pattern memory also recorded in DEV_ISSUES 原则沉淀.
- Backlog cleared:
  (a) AdmissionCutoffMapper.findLatestByProvinceAndSubject — province-global MAX(year) → per-university correlated window (L-06 family root fix). Live: recommendation candidate pool 38 → 1,555 real schools; each school now shows its own latest-year rank (25720/24083/10797… instead of the uniform demo 25204). NOTE: 冲稳保 tiering now operates on a much larger pool — ordering/probability tuning worth a follow-up pass.
  (b) addPlanItem by-name matching: local planner passes schoolName args; resolveSelectedRecommendationItem matches label/universityName in the latest recommendation payload; absent name → explicit guidance ("最近一轮推荐中没有「X」…"). Verified live: extraction clean (把 stripped via schoolStripVerbs/schoolNamePrefixes), 中南大学 correctly rejected as absent from a 25204-rank recommendation instead of silently adding item 1.
  (c) DB_SCHEMA_INIT_MODE default flipped always→never in application.yml/dev/prod (prod `java -jar` no longer DROPs all tables; fresh volumes still bootstrap via MySQL initdb; compose override already pinned never).
  (d) LIKE wildcard escaping in SchoolDetailService fuzzy resolver.
  (e) SchoolsView major-filter failure keeps last filter set + visible inline error (no more silent filter-off).
  (f) MajorDetailView hardcoded "2025 本科批" → dynamic year from actual data rows (falls back 最新年份).
  (g) Display/prompt layer SubjectType.name() → getDisplayName() in AgentDialogState/AgentReplyFormatter/AgentFallbackAdviceService (LLM no longer sees/emits PHYSICS).
  (h) RocketMQ async recommendation pipeline documented as API-only (frontend uses sync flow; failure visibility gap recorded in DEV_ISSUES) — wiring or removal left as product decision.
- Tests 278 green throughout; deployed to zv6-backend (healthy); live smokes: by-name add rejection path, year-window recommendation, failure-rewrite (Harvard), major-schools real-data list — all confirmed.

## 2026-09-21 late night: recommendation ordering ablation study (A0 → A1)
- Method: ablation harness tmp/ablation.py (env-based credentials, UTF-8 client) — pulls full rush/safe/guarantee for 4 profiles (湖南物理588/560/620, 广东物理580) and computes per-bucket quality metrics: rank-impossible count (rankGap < -3000 yet recommended), bucket purity (rankGap within the bucket's model rank window), |rankGap| mean, demo-2026 leakage.
- A0 baseline defects: 264 rank-impossible items across 4 profiles; RUSH purity as low as 18.8-31% (e.g. 陆军工程大学 rank 8890 recommended for a 25204-rank student). Root cause: ProbabilityService blended probability falls back to the score-only basis whenever the rank basis returns null (rank gap beyond ±model range), so a coincidentally low cutoff score rescued rank-impossible schools.
- Tuning (A1): rank one-vote veto in RecommendationPolicyService.explain — when rank data is complete and rankGap < MIN_RANK_GAP, probability = 0 / strategy null / recommended false regardless of the score basis; missing rank data still falls back to score-only. Unit tests: veto case, explain-zero case, missing-rank fallback case.
- Ablation results (A0 → A1): rank-impossible 264 → **0** across all profiles; RUSH purity 31%/23.8%/85.2%/18.8% → **100%/100%/95.8%/100%**; SAFE purity 62.3%/46.3%/65.5%/42.1% → 79.6%/75.5%/74.5%/72.7%; totals shrank ~5-9% (the removed schools were rank-impossible). Residual SAFE impurity (~25%) comes from score-basis-only rows near window edges — acceptable, monitored.
- Fixture consistency fixes exposed by the veto (test-only): H2 curve had no 660 point (extrapolation produced rank 10000, vetoing 浙大 for a 660-scorer) → added 655→5200/660→4800 rows consistent with 浙大 658→5000; 南京师范 江苏 row 612→20000 contradicted 620→26000 mapping → corrected to 612→29000. The veto surfaced these; fixtures were made self-consistent rather than weakening the veto.
- Tests 278 → 282 green (4 new policy tests + fixture alignment); deployed to zv6-backend; ablation A1 re-run against live service confirms metrics. TEMP diagnostics added and removed during diagnosis (RecommendationService/FreeTextRecommendationService/RecommendationControllerTest clean).

## 2026-09-22: click-test wave by main agent + rank-veto frontend regression fix
- Browser registry is main-agent-only (subagent attempt honestly failed with "Browser is not available in subagent", fell back to HTTP checks). Click execution stays with the main agent; analysis delegated to subagents — consistent with the persisted workflow's notCovered note.
- Click suite against live deployment (login flow / schools search filter / 中南大学 detail 52 deduped majors with real data / 智能选大学 total=1630 pagination / agent failure-rewrite UI): all PASS. The failure-rewrite UI renders as designed: failed-tool tag kept + neutral gray "系统数据未命中，已由 AI 结合通用知识补充解答" + LLM answer labeled 通用信息.
- NEW finding via click test: school detail probability panel showed "待测算/未设置分数" for smoketest_jev while tcsmoke0919 saw "0% 概率极低". Root cause (analysis subagent): the frontend extremely-low classifier (utils/recommendation.js isExtremelyLowProbability) only recognized the legacy probability=null + keyword form; the rank-veto state introduced with the ablation tuning returns probability=0 + strategy=null, which fell through to the misleading static "未设置分数" branch. Fixed the classifier to also treat probability=0 && strategy empty as extremely-low; container rebuilt; browser-verified: panel now shows "0% 概率极低" with full rank breakdown for the same account. Tests 282 green (backend unaffected; frontend-only fix).

## 2026-09-22: full-site UI click-test sweep (17 routes)
- Coverage: all 17 frontend routes clicked in a real browser against the live deployment: 首页/登录/查大学列表+搜索/院校详情(4页签:院校概况/近三年录取/开设专业/招生计划)/查专业列表/专业详情(4页签:概况/开设院校/录取预测/相关就业)/志愿填报(两阶段+45志愿位+概率极低卡拒绝+正常卡添加 0→1)/智能选大学(total 1630 分页)/排行(医药类筛选=2所)/一分一段(588→25,204)/招生计划(1630 所全量)/资讯列表+详情(点击跳转+正文)/推荐查询/历史记录(8 条)/志愿方案/画像设置重定向/admin 权限(USER 访问 /admin 被重定向 /recommend)。
- Result: 0 functional failures. Cross-checked against DB: 中南大学开设专业 52 去重、临床医学计划 25 人、医药类排行 2 所、一分一段 588→25204、院校总数 1630 — all consistent.
- Findings (non-blocking): ① 资讯列表条目为 div+JS 跳转而非 <a> 链接（功能可用，可访问性/可分享性弱，P3）；② 招生计划/查大学等超长列表（1630 卡片）一次性渲染无虚拟滚动，DOM 过大导致自动化快照超限，人肉浏览亦建议引入虚拟滚动（P3）；③ 上一轮已修的"未设置分数"回归即本轮点击发现并闭环。
- Note: subagents cannot access the browser registry (platform boundary, attempt honestly failed); UI click execution stays with the main agent, analysis with subagents — consistent with zhiyuan-bug-hunt workflow design.

## 2026-09-22 continued: interaction-level sweep + AI conversation multi-turn test
- Scope per user: 页面跳转正确性 + 全部遴选框/下拉框真实生效 + AI 对话多轮质量。
- Navigation: 6 top-nav buttons clicked sequentially — URL/title all correct (首页/查大学/查专业/志愿填报/智能选大学/高考资讯).
- 查大学 filters exercised for real: 位置下拉(276 选项→北京 61 所；61 vs SQL 62 的 1 所差异为后端 withDataOnly 口径边缘，P3 对账项)、类型下拉(school_type 无"类"后缀：选项为"师范"等；北京+师范→1 所=首都师范大学)、排序切换(96% 降序封顶合理)、专业下拉(计算机科学与技术→4 所北京高校)。全部真实生效。
- 志愿表: 概率极低卡(北大 0%)点添加被正确拒绝并警告；正常卡(40%)添加 志愿表 0→1。
- AI 对话 8 轮多轮测试: 7/8 通过（推荐→加入→查方案→删除确认→确认→概率推荐→专业概览→保存方案《测试方案A》）。抓到 1 个真缺陷并两段修复：
  ① LLM 兜底生成的确认话术带空格("确认删除第 1 个")，hasPendingDeleteConfirmation 按无空格字面匹配失败 → 确认链路断裂。修复：匹配前空白归一化(AgentDialogState)。
  ② 修复后仍断：用户删除请求不带"志愿/方案"名词("删除第1个")时 planNouns 前置条件过严。修复：放宽为删除动词即可（助手确认提示本身已是待确认凭证）。
  修复后 live 5 轮闭环验证：推荐→加入(幂等去重提示正确)→删除确认提示→**removePlanItem 真实执行**→方案 0 条。283 测试全绿；已部署。
- Interaction findings (non-blocking, P3): 头部全局搜索空输入点击也会新建 AI 会话（会话列表噪音）；资讯列表条目非 <a> 链接；招生计划/查大学超长列表无虚拟滚动（DOM 过大导致自动化快照超限）。

## 2026-09-23: school emblem (校徽) backfill for unmapped universities
- Gap found: frontend logo map (utils/schoolLogoMap.js) had only 59 human-verified entries out of 1,902 universities — ~97% of school cards fell back to the first-character placeholder.
- Source: GitHub BadgeSpider (863379105/BadgeSpider) — 933 university emblems scraped from Baidu Baike, files named by school name. Matched against DB university names with normalization (full/half-width parens, spaces): 833 schools coverable.
- Pipeline (tmp/logo_fill/): badge tree cache → name match → concurrent download (raw.githubusercontent, 12 workers, resumable; all 833 OK after retry) → PIL convert to JPEG (white-matte alpha, ≤256px, q88) → frontend/public/logos/{2000+idx}.jpg → SCHOOL_LOGO_IDS extended with 779 entries (54 overlapping human-verified entries kept; 5 human entries without BadgeSpider source retained).
- Map integrity verified via node import: 838 total entries; new (中国科学院大学→2107) resolves; human entries (清华→21, 中山→30) preserved; unknown names → null placeholder. All 833 new files present on disk, zero missing.
- Deployed; served check /logos/2107.jpg = 200 (25KB). Browser acceptance: 查大学 page 753 logo imgs rendered with 0 broken; searching 中国科学院大学 → card shows real emblem from /logos/2107.jpg.
- Coverage after fill: 838/1,902 universities (~44%) show real emblems (was 59/1,902 ≈ 3%); remaining ~1,064 (mostly 高职/民办 absent from the source set) keep the first-character placeholder — acceptable fallback; another source pass could cover more if ever needed.
- Build note: docker `COPY frontend/` layer served stale cache after mass file addition; adding an index.html cache-buster comment forced the rebuild (new logos then packaged; jar verified 1830 logos? final served spot-checks confirm).

## 2026-09-23 final: emblem coverage pushed to 98% (90% target exceeded)
- Breakthrough: rendered www.gaokao.cn/school/{id} in the real browser and extracted the emblem URL pattern — https://static-data.gaokao.cn/upload/logo/{school_id}.jpg (school_id available for all 2,991 schools in the W6-cached 掌上高考 list pages).
- Batch2: 1,058 previously-missing schools → 1,027 had zjzw school_id + logo file (zero download failures); converted/normalized same as batch1 (PIL, white-matte, ≤256px, q88), saved as /logos/3000+.jpg.
- Map: SCHOOL_LOGO_IDS now 1,860 entries (59 human + 779 batch1 + 1,027 batch2 - dedup). 42 universities remain without emblems (1.1%, mostly names absent from both sources) — first-character placeholder retained.
- Deployed to zv6-backend; live verification: /logos/3005.jpg (长沙民政) / 3006 (湖南铁道) / 3003 (湖南工商) all HTTP 200; 查大学 page renders 1,595 logo imgs (of 1,630 cards; 35 unmapped keep placeholder) with zero broken images; 湘潭大学 detail emblem verified earlier.
- Debug artifacts cleaned (TEMP-DEBUG prints, debug tests); 282 tests green at final state.

## 2026-09-23 final+: batch3 via real-browser Baike extraction → 1,901/1,902 (99.9%)
- Last 33 unmapped schools: urllib scraping of Baike was 403-blocked (anti-bot upgrade since 2019); switched to rendering each Baike page in the real browser (IAB) and extracting bkimg.cdn.bcebos.com URLs from the DOM (34 pages incl. retries; 江苏建筑职业技术学院 has no extractable emblem image).
- Downloaded all 33 from bkimg CDN directly (public, no referer check), PIL-normalized, saved as /logos/5000+.jpg; SCHOOL_LOGO_IDS extended → 1,901 entries.
- Misdirection guard: two Baike items redirected to different institutions (大连理工大学城市学院→大连工程学院 post-转设; 江西泰豪动漫→南昌科技职业大学 post-renaming) — accepted as same-lineage successor emblems per user's campus-sharing rule; noted for future data audit.
- Final coverage: 1,901/1,902 mapped (99.9%); only 江苏建筑职业技术学院 keeps the first-character placeholder. Deployed to zv6-backend; live check 长沙民政职业技术学院 card renders /logos/3005.jpg ✓. Tests 283 green.

## 2026-09-23 参赛材料整改交付

- 按 `tmp/materials_text/参赛材料整改问题总清单.md` 更新 01–06 六份 DOCX，重绘推荐概率与 Agent 流程图，替换界面书和手册中的旧截图；08 PPT 保留 29 页并重做 12 页关键内容。交付目录：`D:\智愿ai改版\智愿AI报考平台_参赛资料_整改版`，原版未覆盖。
- 活库重新核实：院校 1,902、专业 657、院校录取线 17,562、专业录取线 166,176（REAL 163,928 / SIMULATED 2,248），专业组投档线 804 均为 SIMULATED。校徽 2,890 个文件；当前测试为 289 项（0 失败、0 错误、1 跳过），意图词典日志显示 29 组。上述两项以当前结果取代审计清单中的 283/27。
- 修复 `sql/schema.sql` 六处乱码 COMMENT，将 `.env.example` 的 `DB_SCHEMA_INIT_MODE` 改为 `never`，避免空卷首次初始化后重复执行 dump 式 schema。源码 ZIP 排除 `.env*` 实密钥、`tmp/`、备份和探测文件；JAR 内含 2,890 张校徽。
- ZIP 的 SHA256 清单验证通过。从 ZIP 独立解压后，以全新 Compose 项目构建并运行，五个常驻服务 healthy，首页 HTTP 200、`testuser` 登录成功，轻量种子实测院校 80、专业 100、用户 7；验证后已关闭测试容器。此前一次测试因复用测试卷的旧密码失败，改用全新项目数据卷后通过。
